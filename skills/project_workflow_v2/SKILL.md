---
name: project_workflow_v2
description: Enter or recover Project Workflow V2 through the canonical runtime-neutral router.
---

This Skill is a thin M01 bootstrap surface.

1. Resolve the installed plugin root that contains this Skill, .codex-plugin, hooks, and workflow.
2. Read the canonical sibling router at <plugin-root>/workflow/ROUTER.md.
3. For an M01 feasibility probe that asks for the router Probe response, return exactly the Probe response value from that router and nothing else.
4. For ordinary work, follow only implemented canonical V2 routing. An unavailable route is a blocker, not permission to invent policy.

Do not copy Project Workflow semantics into this Skill. Do not select semantics from runtime/model/session identity.
