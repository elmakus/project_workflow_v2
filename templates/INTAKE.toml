# TEMPLATE ONLY — exact workstream-local Intake/alignment record.
workstream_id = "example-workstream"
kind = "issue"
state = "active"
diagnosis_revision = 1
repair_subject = "repair:example:v1"
diagnosis_prior_art_subject = ""
diagnosis_prior_art_result = ""
response_kind = "none"
response_observed = false
alignment_state = "pending"
alignment_subject = ""
micro_fix_candidate = false
# When a diagnosis prior-art binding is persisted, also record its exact
# consumed-Research proof locator (RF007 Git identity of the consumed
# RESEARCH.toml blob); omit the table while no binding is persisted.
# [diagnosis_prior_art_proof]
# class = "research"
# repository = "owner/repo"
# commit = "<40-hex>"
# path = "implementation/workstreams/example-workstream/RESEARCH.toml"
# blob = "<40-hex>"
